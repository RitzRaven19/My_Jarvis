"""
critic -- the lawyer. Attacks the weakest link in the proposer's position. Its job
is NOT to be balanced (forced balance produces mush; commitment produces signal).

STUB (Month 0): placeholder critique. Phase 2: a genuinely adversarial pass that
finds the single most load-bearing weakness, in character.
"""
from __future__ import annotations


def critique(state: dict) -> dict:
    # TODO(phase 2): models.chat(ANSWER_MODEL, system=persona+critic-role: attack the
    #   weakest link, do not be balanced).
    return {"critique": "(stub) weakest-link attack on the position"}
